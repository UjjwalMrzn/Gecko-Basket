import React from 'react';
import { Box, Typography } from '@mui/material';

const Recent = () => {
  return (
    <Box p={2}>
      <Typography variant="h4">Recent Component</Typography>
    </Box>
  );
};

export default Recent;
