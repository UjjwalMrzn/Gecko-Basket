import { Box, Typography } from '@mui/material';

const Brand = () => {
  return (
    <Box p={2}>
      <Typography variant="h4">Brand Component</Typography>
    </Box>
  );
};

export default Brand;
