import React from 'react';
import { Box, Typography } from '@mui/material';

const Features = () => {
  return (
    <Box p={2}>
      <Typography variant="h4">Features Component</Typography>
    </Box>
  );
};

export default Features;
