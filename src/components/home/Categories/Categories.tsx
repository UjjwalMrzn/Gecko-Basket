import React from 'react';
import { Box, Typography } from '@mui/material';

const Categories = () => {
  return (
    <Box p={2}>
      <Typography variant="h4">Categories Component</Typography>
    </Box>
  );
};

export default Categories;
