import React from 'react';
import { Box, Typography } from '@mui/material';

const ProductCard = () => {
  return (
    <Box p={2}>
      <Typography variant="h4">Product Component</Typography>
    </Box>
  );
};

export default ProductCard;
