import React from 'react';
import { Box, Typography } from '@mui/material';

const Client = () => {
  return (
    <Box p={2}>
      <Typography variant="h4">Client Component</Typography>
    </Box>
  );
};

export default Client;
